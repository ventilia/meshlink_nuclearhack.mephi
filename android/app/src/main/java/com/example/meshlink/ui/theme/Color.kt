package com.example.meshlink.ui.theme

import androidx.compose.ui.graphics.Color




val PixelBlack      = Color(0xFF0A0A0F)
val PixelDarkGray   = Color(0xFF111118)
val PixelMidGray    = Color(0xFF1A1A28)
val PixelSurface    = Color(0xFF0D0D16)

// Акценты
val PixelAccent     = Color(0xFF00E676)
val PixelAccentDark = Color(0xFF00A854)
val PixelPurple     = Color(0xFF4A3F6B)   // тёмно-фиолетовый — фон баблов входящих
val PixelPurpleLight= Color(0xFF6B5E99)   // светлее фиолетовый — borders

// Предупреждения / звонки (красный вместо оранжевого)
val PixelWarn       = Color(0xFFFF4444)   // красный — ошибки, завершение
val PixelCallGreen  = Color(0xFF00C853)   // зелёный для принятия звонка

// Текст
val PixelText       = Color(0xFFEAEAF0)   // основной текст
val PixelTextDim    = Color(0xFF6B6B80)   // приглушённый текст
val PixelTextHint   = Color(0xFF3D3D52)   // очень тихий текст / плейсхолдер

// Баблы
val PixelBubbleOut  = Color(0xFF1A3D2B)   // исходящие — тёмно-зелёный
val PixelBubbleOutBorder = Color(0xFF00A854)
val PixelBubbleIn   = Color(0xFF1E1B2E)   // входящие — тёмно-фиолетовый
val PixelBubbleInBorder  = Color(0xFF4A3F6B)

// Границы
val PixelBorder     = Color(0xFF252535)   // стандартная граница
val PixelBorderAccent = Color(0xFF2A4A36) // зелёная граница